<!DOCTYPE html>
<html>
  <head>
  	<%= render 'includes/head' %>
  
  </head>

  <% if user_signed_in? %>
    <body>
  <% else %>
    <body class="showcase">
    <% end %>    
  	<%= render 'includes/main_navigation' %>

    
  	<main role="main" class="main container">
        	<%= render 'includes/flash_messages' %>

    	<%= yield %>
    </main>

    <%= render 'includes/footer' %>
    <%= render 'includes/comments_destroy_modal' %>
  </body>
</html>
